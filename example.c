#include <stdio.h>

int newfunc(int a, int b)
{
    printf("Hi");
}
int main()
{
    int x, y, arr[50], arr_new[50]; // new comment
    if (x == y)
    {
        x = 5;
    }

    printf("hello world"); // hello this is me
    char a;
    x = 10 + 5;
}
float secondfunc(int c, int d, char f[50])
{
    printf("Whole New world");
}